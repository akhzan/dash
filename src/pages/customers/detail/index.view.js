import { Form } from 'antd'
import React from 'react'

const CustomerDetailView = ({ form }) => {
  return (
    <Form form={form}>
      <div>Detail</div>
    </Form>
  )
}

export default CustomerDetailView
